```c
#include <stdio.h>
int main(void) {
    printf("HelloCHighlight");
    return 0;
}
```

```python3:interactive
def main():
    print("HelloPyInteractive")
main()
```

```python:interactive:button-name:AliasPy
def main():
    # Assembled from fragments so the expected output does not appear literally in the
    # displayed source, which would make "should not see" pass before the code is run.
    parts = ("Alias", "Py", "Ran")
    print("".join(parts))
main()
```
