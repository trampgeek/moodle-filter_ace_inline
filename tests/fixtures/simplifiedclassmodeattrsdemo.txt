<!-- These two blocks (line-numbers, dark-theme-mode) are no longer each
individually asserted on for their own attribute effect - that's covered by
the generated tests/behat/scenarios/ suite now - but both are still needed
here as page-order context for the "earlier block's line-numbers does not
leak into a later block" regression test below, which is unique to this
multi-block-per-page fixture and can't be reproduced by the generated
suite's one-fixture-per-page architecture. -->

```c:line-numbers:5
int alpha;
int beta;
```

```c:dark-theme-mode:2
int darkforcec;
```

```python3:interactive:dark-theme-mode:0
lightforcepy = 1
```
